<Window x:Class="Arkangel.Target_ByApp"
       xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Arkangel"
        mc:Ignorable="d"
        Title="By Application" Height="200" Width="500"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
     TextElement.Foreground="{DynamicResource MaterialDesignBody}"
     TextElement.FontWeight="Regular"
     TextElement.FontSize="20"
     TextOptions.TextFormattingMode="Ideal"
     TextOptions.TextRenderingMode="Auto"
     WindowStartupLocation="CenterScreen"
     Background="{DynamicResource MaterialDesignPaper}"
     FontFamily="{DynamicResource MaterialDesignFont}"
        Icon="Sources\logo_LSC_icon.ico"
        WindowStyle="ToolWindow">
    <Grid>
        <StackPanel Orientation="Horizontal">
            <ListBox x:Name="list_app" Width="380"></ListBox>
            <StackPanel>
                <Button x:Name="bt_add" Margin="20,30" Width="75" Click="bt_add_Click">Add</Button>
                <Button x:Name="bt_cancel" Margin="20,20" Width="75" Click="bt_cancel_Click">Cancel</Button>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
