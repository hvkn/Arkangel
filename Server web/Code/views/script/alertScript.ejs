<script src="https://code.jquery.com/jquery-3.3.1.min.js"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.14.3/umd/popper.min.js"></script>
<script src="https://stackpath.bootstrapcdn.com/bootstrap/4.1.1/js/bootstrap.min.js"></script>
<script type="text/javascript" src="https://cdn.jsdelivr.net/npm/toastify-js"></script>
<script src="javascripts/owl.carousel.min.js"></script>
<script src="javascripts/dropzone.min.js"></script>
<script>
    $(document).ready(function() {
        if(<%= data.enableEmail %> === true && <%= data.enableEmail %> !== null )
            $('#enableEmail').prop('checked', true);
        if (<%= data.enableScreenshot %> === true)
            $('#enableScreenshot').prop('checked', true);

        if ("<%= messageSave.length %>" > 0) {
            Toastify({
                text: "<%= messageSave %>",
                duration: 10000,
                newWindow: true,
                close: true,
                gravity: "top", // `top` or `bottom`
                positionLeft: false, // `true` or `false`
                backgroundColor: "linear-gradient(to right, #00b09b, #96c93d)",
            }).showToast();
        }

        if ("<%= messageAdd.length %>" > 0) {
            Toastify({
                text: "<%= messageAdd %>",
                duration: 10000,
                newWindow: true,
                close: true,
                gravity: "top", // `top` or `bottom`
                positionLeft: false, // `true` or `false`
                backgroundColor: "linear-gradient(to right, #00b09b, #96c93d)",
            }).showToast();
        }

        if ("<%= messageDel.length %>" > 0) {
            Toastify({
                text: "<%= messageDel %>",
                duration: 10000,
                newWindow: true,
                close: true,
                gravity: "top", // `top` or `bottom`
                positionLeft: false, // `true` or `false`
                backgroundColor: "linear-gradient(to right, #00b09b, #96c93d)",
            }).showToast();
        }

        $('.navbar-menu a[href*="/alert"]').addClass('active-item');
    })
</script>
<script src="javascripts/script.js"></script>